# Copy to .env and fill in from your Turso dashboard (dashboard.turso.tech):
# select your database -> Connect / Auth Token to get these two values.
# .env is gitignored -- never commit real credentials.

TURSO_DATABASE_URL=libsql://your-db-name-your-org.turso.io
TURSO_AUTH_TOKEN=
